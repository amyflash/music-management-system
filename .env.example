# 数据库配置
DB_USER=musicuser
DB_PASSWORD=your_secure_password_here
DB_NAME=musicdb

# 数据库连接 URL（自动生成，无需修改）
# DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}

# 应用配置
NODE_ENV=production
PORT=5000

# 对象存储配置（可选，用于 Serverless 环境）
# 如果部署到 VPS，可以忽略以下配置
# OSS_REGION=
# OSS_ACCESS_KEY_ID=
# OSS_ACCESS_KEY_SECRET=
# OSS_BUCKET=
# OSS_ENDPOINT=

# ImgBB 配置（可选，用于临时图片存储）
# IMGBB_API_KEY=
